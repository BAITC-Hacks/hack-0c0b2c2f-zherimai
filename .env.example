# Core pipeline, viewer and local assistant require no environment variables or API keys.
# Optional CLI planner: export these locally; .env is not loaded automatically.
# OPENAI_API_KEY=your_project_key
# OPENAI_MODEL=gpt-5.4-nano-2026-03-17
# Never commit a real key or paste it into a chat.
