# Фактические проверки 23 сентября 2026

Среда: macOS ARM64, Python 3.13.2; версии пакетов закреплены в `requirements.txt`. Проверен выданный набор parquet, не искусственные готовые ответы. Следующий блок относится к исходной версии `3884d25`; обновление ниже отделяет новые проверки от исторических.

- Полный расчёт из parquet: 2 248 уникальных gid, 3 обязательных и 2 дополнительных CSV, автономный HTML, report.json; валидатор PASS.
- Роли: 21 coordinator, 42 consolidator, 43 distributor, 36 transit, 566 terminal, 1 540 peripheral. Ни один узел depth=4 не получил terminal. Сохранены все 19 изолятов.
- 49 групп: 48 активных сообществ и техническая группа 0. Все размеры, seed и внутренние обороты сверены с исходными рёбрами.
- 10 содержательных тестов успешно выполнены: временные границы, недопустимость повторного использования суммы, обрыв обхода, seed-баланс, повреждение агрегатов и покрытие gid.
- Отдельная чистая папка без out и .venv: создан новый venv, пакеты установлены только через `--no-index --find-links wheelhouse`. `pip check` прошёл.
- Первый полный запуск нового процесса в чистом окружении: **15,71 секунды** по внешнему таймеру, повторный: **3,21 секунды**. Начальная установка пакетов сюда не включена. Таймер `run.py` впоследствии перенесён перед импортом сторонних библиотек, чтобы также учитывать их стоимость.
- Все 5 CSV побайтно совпали между рабочим и чистым окружением, а также при повторном прогоне.
- Валидатор/загрузчик отвергли 8 повреждений: отсутствующая обязательная выходная колонка; пустой evidence; evidence без цифр; evidence длиннее 200; terminal на depth=4; неизвестная роль; score>1; отсутствующая входная date.
- Проверены точные строковые gid в HTML и действия JavaScript на фактическом наборе с DOM-заглушкой: поиск координатора, изолята и глубины 4, переход на 2 шага, неизвестный gid, помощник, фильтр роли. Проверена синтаксическая корректность JavaScript.

## Проверка расширенной версии

- Подключён `680769e` тиммейта Alexandr. Его сохранённый отчёт: 2,4757 с, Python 3.14.7, pandas 2.3.3, pyarrow 23.0.1. Это сведения из коммита, не собственная проверка второй ОС или установка наших закреплённых версий.
- Расширенный расчёт создаёт 7 CSV, HTML и report.json. Рабочий прогон — **4,1372 с**, предыдущий контрольный — 3,8376 с; валидатор PASS. Новые временные границы согласованы у всех 2 248 узлов.
- С исходным сохранённым результатом побайтно по значениям совпадают gid, role, base_role, role_score, priority_score и cluster_id. Пороговая методика и исходная очередь не изменялись.
- **49 тестов проходят**, в том числе при включённом контроле ResourceWarning. Проверены дополнительные временные границы, чувствительность, полнота объяснений и 20 сценариев optional AI с подменой HTTP.
- Независимый `scripts/audit_top.py` после пересчёта не обнаружил расхождений, включая новые временные reach. Максимальная ошибка временной доли — 4,98×10⁻¹³; максимальный evidence — 124 символа; строк, обрезанных на 200, нет.
- Независимое ревью optional API выявило и закрыло три дефекта: подмена явно указанного номера кластера, отсутствие fallback при отдельных HTTP-обрывах, отсутствие ссылок на рёбра общих получателей. Реальных API-вызовов этого модуля не было; качество реальной модели не измерено.
- Затем расширенная версия проверена в отдельной папке с новым venv и offline установкой закреплённых wheels. Холодный процесс — 19,42 с; все 7 CSV и HTML побайтно совпали, 49 тестов и независимый аудит прошли. Команды и среда: [verification-update.md](verification-update.md).
- Позже выполнена отдельная живая проверка `gpt-5.4-nano-2026-03-17`: 12/12 случаев (11 онлайн-планов и один локальный отказ). С исходным smoke — 12 реальных API-вызовов; журнал и usage проверены. Подробности и ограничения малого набора: [ai-assistant.md](ai-assistant.md), результаты: [ai-eval-results.json](ai-eval-results.json). Это обновляет исторический статус «реальный API не проверен», не меняет offline-проверку ядра.

## Что ещё не проверено

- Визуальный просмотр текущего объединения: капитан передал подтверждение Александра для UI `3ff778d`. Также передан отзыв Claude о Chromium/Linux без ошибок и сетевых запросов, однако точный SHA неизвестен. Эти сообщения не подтверждают визуальную проверку более нового main или запуск Python-пайплайна на Linux. Инструмент Browser Use отклонил доступ к локальному файлу. DOM-проверки новой версии не заменяют ручной просмотр; участнику B нужно открыть свежий `out/network.html` и пройти сценарий `docs/demo.md`.
- Linux-пайплайн и чистая установка Linux не проверены. Для Windows Ержан теперь подтвердил чистую установку зависимостей из локальных wheels на ca8b43d; подробности: [offline-windows-erzhan.md](handoffs/offline-windows-erzhan.md). Новый main после этой версии требует повторной независимой приёмки. Установщик Python в offline-комплект не включён; wheels вне Git и зависят от ОС/архитектуры.
- Капитан подтвердил выбранный кейс «Граф денег» и включение данных в официальный репозиторий. Финальный push и отдельная подача остаются последующими действиями.
- Размеченных истинных ролей нет; качество классификации (precision/recall/accuracy) не измерялось.

Для воспроизведения: `python run.py`, `python validate.py`, `python -m unittest discover -s tests`. При новых изменениях повторять проверки затронутой логики.

## Приёмка работ B/C и исправления по QA

Коммиты Александра d553d83/3ff778d и отчёт Ержана 108e2a4 объединены с сохранением авторства. Последние результаты расчёта, прямых подмен валидатора, чистого запуска и карточки: [verification-integration.md](verification-integration.md). Исторический отчёт C описывает найденные проблемы до исправления; их закрытие фиксируется отдельно.

## Подготовка Python и UTF-8 после внешнего ревью

Проверен полный путь uv с реальной загрузкой CPython 3.13.15 в отдельную пустую среду: холодный расчёт 15,009 с, 90 тестов, валидатор и аудит PASS; 7 CSV и HTML совпали. Протокол: [uv-verification.md](uv-verification.md). В рабочей среде также выполнены run.py, validate.py и scripts/analyze.py с предупреждением неявной кодировки как ошибкой; кириллический отчёт разведки прочитан как UTF-8, детерминированные выходы сохранены.

## Проверка объединения перед технической сдачей

После включения C 715cd5c и исправления объяснений карточки: полный рабочий расчёт 4,1824 с; 95 тестов PASS. В ещё одной новой копии без out, с ранее отдельно загруженным CPython 3.13.15 и закреплёнными зависимостями: расчёт 4,145 с по внешнему таймеру, отдельный валидатор, 95 тестов, независимый аудит и DOM-проверка PASS. Все 7 CSV и HTML побайтно совпали с рабочим результатом. Расчёт, валидатор, тесты и аудит выполнены через Python socket QA-guard; он не зарегистрировал сетевых попыток в контролируемых процессах. Это не проверка системного firewall или визуального браузера. [Машинный протокол](pre-submit-verification.json).

Карточки и скачиваемый текст теперь показывают structural_neighbors, betweenness и порядок выбора ролей для любого gid. Это позволяет объяснить, почему coordinator не сработал при выполнении части его условий. Проверены 7 карточек/экспортов в переносимом scripts/check_viewer.cjs и отдельно 11 сценариев по всем 6 ролям. Исходные метрики сверены с parquet, роли и scores независимо проверены по README. [Подготовленные ответы](rehearsal.md); человеческая репетиция за минуту ещё не подтверждена.
