# Фактические проверки 23 сентября 2026

Среда: macOS ARM64, Python 3.13.2; версии пакетов закреплены в `requirements.txt`. Проверен выданный набор parquet, не искусственные готовые ответы.

- Полный расчёт из parquet: 2 248 уникальных gid, 3 обязательных и 2 дополнительных CSV, автономный HTML, report.json; валидатор PASS.
- Роли: 21 coordinator, 42 consolidator, 43 distributor, 36 transit, 566 terminal, 1 540 peripheral. Ни один узел depth=4 не получил terminal. Сохранены все 19 изолятов.
- 49 групп: 48 активных сообществ и техническая группа 0. Все размеры, seed и внутренние обороты сверены с исходными рёбрами.
- 10 содержательных тестов успешно выполнены: временные границы, недопустимость повторного использования суммы, обрыв обхода, seed-баланс, повреждение агрегатов и покрытие gid.
- Отдельная чистая папка без out и .venv: создан новый venv, пакеты установлены только через `--no-index --find-links wheelhouse`. `pip check` прошёл.
- Первый полный запуск нового процесса в чистом окружении: **15,71 секунды** по внешнему таймеру, повторный: **3,21 секунды**. Начальная установка пакетов сюда не включена. Таймер `run.py` впоследствии перенесён перед импортом сторонних библиотек, чтобы также учитывать их стоимость.
- Все 5 CSV побайтно совпали между рабочим и чистым окружением, а также при повторном прогоне.
- Валидатор/загрузчик отвергли 8 повреждений: отсутствующая обязательная выходная колонка; пустой evidence; evidence без цифр; evidence длиннее 200; terminal на depth=4; неизвестная роль; score>1; отсутствующая входная date.
- Проверены точные строковые gid в HTML и действия JavaScript на фактическом наборе с DOM-заглушкой: поиск координатора, изолята и глубины 4, переход на 2 шага, неизвестный gid, помощник, фильтр роли. Проверена синтаксическая корректность JavaScript.

## Что ещё не проверено

- Реальный визуальный просмотр в браузере: инструмент Browser Use отклонил доступ к локальному файлу. Программные проверки интерфейса не заменяют ручной просмотр. Участник B должен открыть `out/network.html` и пройти сценарий `docs/demo.md`.
- Windows/Linux: код и инструкции подготовлены, но реальная проверка проведена только на текущем macOS. Локальный wheelhouse соответствует macOS ARM64 / Python 3.13 и не входит в Git. Для иной целевой среды нужны совместимые пакеты.
- Капитан подтвердил выбранный кейс «Граф денег» и включение данных в официальный репозиторий. Финальный push и отдельная подача остаются последующими действиями.
- Размеченных истинных ролей нет; качество классификации (precision/recall/accuracy) не измерялось.

Для воспроизведения: `python run.py`, `python validate.py`, `python -m unittest discover -s tests`. При новых изменениях повторять проверки затронутой логики.
