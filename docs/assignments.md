# Назначения команды

**Актуальное уточнение капитана после выпуска `5926dbd`, 17:35:** финальную редакцию `README.md` делает **Саша / Alexandr**. A не редактирует README параллельно; принимает результат, проверяет команды/ссылки/фактические утверждения и интегрирует его до дедлайна. A разбирает замечания внешнего ревью и готовит отдельные материалы защиты без новых функций. Это назначение имеет приоритет над прежним распределением README ниже.

Назначено 23 сентября 2026 по просьбе капитана немедленно распределить работу. Имена Alexandr и Erzhan взяты из истории официального репозитория; Git-авторство каждого остаётся его собственным. Это актуальное распределение вместо прежнего «решим позже».

| Кто | Область | Ветка | Срок первой передачи |
|---|---|---|---|
| Капитан a4ns + его Codex | A: методика, приоритеты и интеграция | main для интеграции; отдельная ветка при необходимости | Следующий проверенный результат до 15:30 |
| Alexandr + его Codex | B: интерфейс аналитика | codex/ui-alexandr | Результат визуальной проверки и первые исправления до 15:30 |
| Erzhan + его Codex | C: воспроизводимость и независимая проверка | codex/qa-erzhan | Чистый запуск или конкретная причина сбоя до 15:30 |

## A — капитан a4ns и текущий Codex

Владеет `aml/features.py`, `aml/roles.py`, `aml/priority.py`, `aml/clusters.py`, `aml/explain.py`, `aml/resilience.py`. Общие файлы `run.py`, `requirements.txt`, README, AGENTS, TEAM_CONTEXT, PLAN, TASKS и финальные `out/` также здесь.

Следующая задача: аудит топ-20 по первичным метрикам, проверка объяснений и чувствительности приоритета/кластеров. Сохранять обязательные схемы и воспроизводимость. Принимать проверенные изменения B/C, обновлять итоговые результаты и описание. Не переделывать UI параллельно с Alexandr. Финальный push и отдельную подачу на платформе контролирует капитан.

## B — Alexandr и его Codex

Владеет только `aml/viewer.py`, `aml/viewer_template.html` и при необходимости отдельными UI-проверками в `tests/test_viewer.py`. Функция `build_viewer(nodes_df, edges_df, clusters_df, out_path)` и строковые gid сохраняются.

Первое действие: получить актуальный main, прочитать AGENTS/TEAM_CONTEXT/README и открыть настоящий экран локально. При явном запрете инструмента браузера не обходить его: человек может проверить экран сам и сообщить наблюдения.

Задача до 15:30:

1. Проверить поиск произвольного координатора, изолированного seed и узла depth=4, выбранных из текущего CSV.
2. Проверить направление стрелок, входящие/исходящие связи, переход к соседу, фильтры ролей/кластеров и 1–2 шага.
3. Убедиться, что по карточке можно за минуту объяснить три названных gid: правило, фактические числа, ограничение и следующий запрос.
4. Исправить найденные ошибки и мешающие чтению элементы; основа уже работает, переписывать интерфейс с нуля не нужно.
5. Передать commit, список проверенных действий, фактические дефекты/исправления и скриншот при разрешённом доступе. Не объявлять программную DOM-проверку визуальным просмотром.

Проблему расчёта/порогов передать A, не менять их в своей ветке. Локальный вывод — `out-check/ui`, итоговый `out/` не коммитить.

## C — Erzhan и его Codex

Владеет `aml/load.py`, `aml/validate.py`, `validate.py`, `tests/test_invariants.py` и новыми отдельными QA-тестами, затем `aml/assistant.py`. Общие зависимости/README самостоятельно не менять одновременно с A: передать точное предложение интегратору.

Задача до 15:30:

1. Получить актуальный main и повторить команды README в новом окружении на своём компьютере. Зафиксировать ОС, архитектуру, Python, команды и время. Если компьютер также macOS, не называть это проверкой Windows/Linux.
2. Проверить все 2 248 gid, изоляты, scores, evidence, согласованность кластеров, топ и запрет terminal на depth=4.
3. Выполнить существующие тесты. Добавлять только тест на конкретный найденный пробел, а не дублировать формулы.
4. Проверить помощника: неизвестный gid, путь, общие получатели, кластер, неподдерживаемый вопрос; никаких выдуманных ответов.
5. Передать commit исправлений или отчёт с фактическим результатом. При проблеме установки указать точную ошибку и совместимый вариант; версии не обновлять вслепую.

После прохождения P0 можно улучшать локальный помощник. Подключение внешнего LLM — отдельный следующий этап с проверяемым доступом, согласованным объёмом данных и сохранением offline режима; сейчас не тратить время на ожидание ключа. Локальный вывод — `out-check/qa`.

## Общая передача

Каждый работает от собственного Git-аккаунта и сообщает капитану: ветку/commit, изменённые файлы, проверки, ограничения. Детали — `docs/team-workflow.md`. Агент не видит чаты других участников: всё важное передать коммитом или коротким сообщением капитану. Ветки отправлять в этот же официальный репозиторий. Не использовать force push и не переписывать чужие изменения.

## Передача после первой интеграции

Получить свежий main перед дальнейшими правками. Роли, scores и основной порядок очереди сохранены; добавлены диагностические колонки и отчёты. Старые сроки первой передачи выше остаются историческими, новый ориентир для следующего результата — **16:00**.

**B / Alexandr:** коммиты `b2853ea` и `680769e` приняты. В Git пока виден результат расчёта, но не перечень действий визуальной проверки. Передать фактическую проверку экрана. В карточке показать новые поля: `matched_roles`, `next_request`, `temporal_seed_reach_strict_days`, `temporal_seed_reach_upper`, `cluster_stability` / `cluster_stability_scope`, `priority_top20_frequency`, диапазон `priority_min_rank`–`priority_max_rank`. Подписать старое `seed_reach` как «структурно, без дат». Для temporal — только совместимость пути с датами, не происхождение денег; isolate_not_assessed не показывать как 100% устойчивость. Осторожная подача важнее количества карточек. Optional API в браузер не подключать и ключ там не хранить; он отдельный CLI.

**C / Erzhan:** повторить чистый запуск расширенного main. Теперь создаются 7 CSV, а `sensitivity.csv` содержит намеренно пустые поля, неприменимые к семейству сценария; это дополнительный отчёт, обязательные CSV остаются полными. Проверить независимый `.venv/bin/python scripts/audit_top.py`, неизменность gid и ролей. Основной `aml.assistant` остаётся за C. Новый `aml.ai_assistant` вёл A с независимым ревью; не переписывать его одновременно. Реальная модель без ключа не считается проверенной.

**A:** контролирует интеграцию, чистую воспроизводимость и содержательный push. Общие README, out и зависимости изменяет только интегратор. Не менять версии пакетов лишь потому, что чей-то существующий venv отличается.

**Обновление A, 15:45:** live-проверка optional API закрыта на gpt-5.4-nano-2026-03-17: 12/12 подготовленных случаев, из них 11 через API и один локальный отказ. Результаты — `docs/ai-eval-results.json`, повторяемая команда — `scripts/evaluate_ai.py --online` при своих переменных окружения. B/C не нужно повторять платный набор; до 16:00 приоритет остаётся у экрана и независимого запуска. Ключ в репозиторий и браузер не переносить.

## Передача после приёмки B/C, ориентир до 16:30

Интегратор принял UI `d553d83`/`3ff778d`, QA-отчёт `108e2a4` и исправление CLI-помощника `ca8b43d` с сохранением авторства. Обновлённая карточка включает недостающие диагностические поля; валидатор теперь сверяет признаки с исходными parquet и проверяет диагностические CSV. Сначала получить свежий main безопасным способом из team-workflow; старые ветки содержат уже выполненные задачи.

**B / Alexandr:** открыть свежий `out/network.html` в настоящем браузере. Проверить сразу открытую карточку лидера и видимый «Состав приоритета», поиск полного gid, изолят, depth=4, два шага, стрелки и фильтры. Проверить строгие команды встроенного помощника; `node scripts/check_viewer.cjs` доступен как дополнительная программная регрессия, не замена просмотру. По трём произвольным gid назвать правило, два числа и ограничение за минуту. Передать точный commit, ОС/браузер и фактический результат в свой handoff; при дефекте — минимальное исправление UI. DOM-заглушку не называть визуальным просмотром.

**C / Erzhan:** на свежем main повторить именно девять отрицательных сценариев своего QA и общий набор тестов, затем один расчёт и независимый audit_top на Windows. Проверки уже добавлены в `tests/test_source_validation.py`; текущий production-валидатор не переписывать без нового воспроизводимого дефекта. Дополнить собственный handoff новой датированной секцией, сохранив исторический результат. Если доступны совместимые wheels, отдельно проверить offline установку в новом venv; отсутствие такого комплекта назвать ограничением, не заменять версии.

**A:** обновить out, результаты приёмки и общий статус; отправить проверенный main. Дополнительные API-вызовы сейчас не нужны. Дальше — исправления реальных замечаний B/C и подготовка технической сдачи к 17:15.

## Уточнение после внешнего ревью

A уже проверил автоматическую подготовку Python 3.13 через uv в свежей копии на macOS; инструкция и протокол в README и docs/uv-verification.md. Также внесены явные UTF-8 чтение/запись. Не дублировать эти правки. C: при следующей независимой проверке Windows воспроизвести именно новый путь uv, если доступна сеть для подготовки; затем расчёт без загрузок. Не расширять поддержку до 3.12 на основании одних метаданных.

B сообщил капитану, что UI 3ff778d проверен; коммит уже находится в main. Следующий просмотр относится к новому main с объединёнными диагностическими полями и строгими командами помощника.

Капитан: подготовленный текст для формы — docs/submission.md. Целевой срок подачи 17:30; перед нажатием уточнить, фиксирует ли форма ссылку, commit или архив. Официальный дедлайн 18:00 остаётся неизменным.

Передача C 715cd5c (чистая Windows-установка из wheels) принята, спасибо за отдельный протокол. Дальше C проверяет последний main и девять подмен валидатора. Если offline-ZIP будет нужен для сдачи, заменить в нём исходники ca8b43d актуальным проверенным main, оставив совместимые wheels, и повторить запуск; старый ZIP не объявлять финальным.

B: в карточку интегратор добавил две ранее скрытые метрики для остальных ролей и порядок правил. Нужны при просмотре также distributor/peripheral, которым coordinator не назначен; вычисленные роли/приоритеты не изменились.


## Последний этап после приёмки 54d1a71 и 5eb989d

Обе передачи приняты, 98 тестов и повторный расчёт из свежей папки прошли. Девять подмен валидатора C уже независимо проверил — повторять их без изменения кода не требуется. Новые функции не добавляем.

- **B / Саша:** получить свежий main, открыть out/network.html человеку и пройти поиск, карточку, входящую/исходящую связь, фильтр и скачивание TXT. Проверить узкое окно, а также distributor/peripheral с объяснением, почему не coordinator. Сообщить SHA, браузер и фактический результат; DOM не заменяет просмотр.
- **C / Ержан:** провести с командой разбор трёх произвольных gid за минуту по docs/rehearsal.md и docs/handoffs/rehearsal-erzhan.md. Если для сдачи понадобится offline-ZIP, обновить его исходники финальным main и проверить запуск; текущий комплект содержит c31e093.
- **A / капитан:** самостоятельно подать по docs/submission.md до 17:30, сохранить подтверждение и сведения о версии. После отправки сообщить факт интегратору, чтобы статус был записан. Официальный предел 18:00 не изменился.
