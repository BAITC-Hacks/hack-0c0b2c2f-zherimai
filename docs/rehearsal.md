# Независимая подготовка к вопросам жюри

Проверена версия входных данных/CSV main c31e093 и последующая узкая правка карточки в рабочей папке. Это подготовленные короткие ответы для репетиции, **не проведённая репетиция и не измеренный человеческий результат**. Интерфейс проверен DOM-заглушкой, не визуально в браузере.

## Что сверено

- 11 сценариев / 10 уникальных gid: все шесть основных ролей, граница depth=4, изолят, seed с исходящими, конкурирующие роли, обе ветки terminal.
- Gid выбраны по условиям/приоритету из текущего результата, не заданы в логике продукта. Степени, суммы, depth/is_seed и структурные соседи сверены непосредственно с parquet; порядок ролей и scores отдельно посчитаны по формулам README без импорта классификатора.
- q99 betweenness = 0.0019892319141410018. Координатор одновременно требует вход/выход >0, ≥2 соседей с in_deg≥5 или out_deg≥10, seed_reach≥3, betweenness≥q99 и >0. Минимальная betweenness реально назначенного coordinator = 0.00199313902353.
- Сила правила совпала после округления до 6 знаков; пять вкладов priority и множитель seed совпали после округления до 8 знаков. Основная роль выбирается coordinator → consolidator → distributor → transit → terminal → peripheral.
- На текущих данных нет consolidator на depth=4; поправка его score ×0,8 существует в правиле, но пример на реальных данных показать нельзя. 55 terminal имеют ненулевые исходящие: это вторая ветка правила, не ошибка.

## Подготовленные ответы

### coordinator: 100000000331309100

Роль `coordinator`; role_score **1.000000**; priority **0.92842527**; depth=2; seed=False. Вход 984 635 ₸ от 5; выход 23 001 375 ₸ к 99. Соседи=11; bw=0.004557234; reach структурный/строгие даты/верхняя граница=7/1/1.

Есть 5 плательщиков и 99 получателей, 11 соседних узлов сбора/рассылки при пороге 2. Достижим от 7 seed по структуре, центральность 0,004557 выше q99≈0,001989. Поэтому coordinator. Это гипотеза связности: по датам совместим путь только от одного seed.

### consolidator: 100000003115284100

Роль `consolidator`; role_score **0.650000**; priority **0.88105092**; depth=1; seed=False. Вход 2 160 500 ₸ от 8; выход 517 000 ₸ к 2. Соседи=7; bw=0.000127805; reach структурный/строгие даты/верхняя граница=11/6/7.

Восемь разных плательщиков превышают порог 5; вход 2 160 500 ₸. Поэтому признаки консолидации. Хотя структурных соседей 7 и seed 11, центральность 0,000128 ниже порога coordinator. Выход 517 000 ₸ не доказывает удержание остатка.

### distributor: 100000005933757100

Роль `distributor`; role_score **0.900000**; priority **0.81658862**; depth=2; seed=False. Вход 1 246 000 ₸ от 4; выход 3 002 628 ₸ к 34. Соседи=1; bw=0.003892788; reach структурный/строгие даты/верхняя граница=7/0/1.

Переводит 34 получателям при пороге 10. Плательщиков 4, поэтому порог consolidator не достигнут. Центральность 0,003893 высокая, но сосед сбора/рассылки только один: для coordinator нужно два. Выход больше входа — запрашиваем недостающие поступления и начальный остаток.

### transit: 100000000201868100

Роль `transit`; role_score **1.000000**; priority **0.72790790**; depth=2; seed=False. Вход 190 000 ₸ от 1; выход 190 000 ₸ к 1. Соседи=0; bw=0.000000000; reach структурный/строгие даты/верхняя граница=7/0/0.

Не seed, один плательщик и один получатель. Вход и выход по 190 000 ₸: отношение 1 попадает в 0,8–1,2 и вход больше 50 тысяч. Это признаки транзита. Сопоставление за 1–2 дня — 100%, но происхождение тех же денег не доказано.

### terminal: 100000003404627100

Роль `terminal`; role_score **1.000000**; priority **0.71385662**; depth=1; seed=False. Вход 877 700 ₸ от 4; выход 0 ₸ к 0. Соседи=0; bw=0.000000000; reach структурный/строгие даты/верхняя граница=11/4/4.

Глубина 1, вход 877 700 ₸, видимых исходящих нет; порог входа 50 тысяч выполнен. Поэтому конечный получатель в этой выборке. Это не окончание всех потоков: дальше нужны наличные, межбанк и операции ниже 5 тысяч.

### peripheral: 100000007629096100

Роль `peripheral`; role_score **0.500000**; priority **0.60301413**; depth=2; seed=False. Вход 1 075 170 ₸ от 4; выход 765 205 ₸ к 1. Соседи=4; bw=0.000015257; reach структурный/строгие даты/верхняя граница=7/1/1.

Четыре плательщика меньше пяти, один получатель меньше десяти. Отношение 0,712 вне транзитного диапазона и выше порога удержания 0,2. Хотя соседей сбора/рассылки 4, центральность 0,000015 ниже q99. Ни одно правило не прошло; peripheral не означает низкий риск.

### depth4: 100000002622822100

Роль `peripheral`; role_score **0.250000**; priority **0.53876334**; depth=4; seed=False. Вход 750 000 ₸ от 1; выход 0 ₸ к 0. Соседи=0; bw=0.000000000; reach структурный/строгие даты/верхняя граница=9/0/3.

Вход 750 тысяч от одного плательщика, выход в выгрузке нулевой. Но это четвёртое колено: исходящие не выгружались. Поэтому нельзя назначить terminal; остаётся peripheral со слабым score 0,25. Следующий шаг — запрос исходящих пятого колена, наличных и межбанка.

### isolate: 100000000456947100

Роль `peripheral`; role_score **0.250000**; priority **0.01886077**; depth=0; seed=True. Вход 0 ₸ от 0; выход 0 ₸ к 0. Соседи=0; bw=0.000000000; reach структурный/строгие даты/верхняя граница=0/0/0.

Это известный seed без единого наблюдаемого ребра и без сумм. Он сохранён в технической группе 0; данных для поведенческой роли нет, поэтому peripheral и score 0,25. Устойчивость сообщества не оценивалась. Приоритет дополнительно умножен на 0,6 как для любого seed.

### seed_with_out: 100000003684369100

Роль `coordinator`; role_score **0.578885**; priority **0.56861307**; depth=0; seed=True. Вход 3 848 436 ₸ от 24; выход 8 588 655 ₸ к 62. Соседи=15; bw=0.002303075; reach структурный/строгие даты/верхняя граница=9/3/3.

У известного seed 24 плательщика, 62 получателя, 15 соседних узлов сбора/рассылки и связь от 9 других seed. Центральность 0,002303 выше q99: coordinator. Сработали также сбор и рассылка. Приоритет 0,947688 до скидки, затем ×0,6=0,568613; неполный баланс seed не трактуем как транзит.

### multi_role: 100000000331309100

Роль `coordinator`; role_score **1.000000**; priority **0.92842527**; depth=2; seed=False. Вход 984 635 ₸ от 5; выход 23 001 375 ₸ к 99. Соседи=11; bw=0.004557234; reach структурный/строгие даты/верхняя граница=7/1/1.

Пять плательщиков дают consolidator, 99 получателей — distributor; одновременно выполнен coordinator. Правила применяются в порядке coordinator → consolidator → distributor, поэтому основная роль coordinator, а остальные сохранены в «Все сработавшие правила». Нельзя скрывать веерные исходящие только из-за одной основной роли.

### terminal_retention: 100000003360542100

Роль `terminal`; role_score **0.796667**; priority **0.70418564**; depth=2; seed=False. Вход 317 000 ₸ от 2; выход 51 800 ₸ к 2. Соседи=1; bw=0.000000991; reach структурный/строгие даты/верхняя граница=11/4/7.

Здесь terminal не означает нулевой выход: есть два получателя. Вход 317 тысяч выше порога 200 тысяч, наблюдаемый выход 51 800, то есть 16,3% — меньше 20%. Узел не seed, глубина меньше четырёх; сработала ветка удержания в видимой выборке.

## Найденное и исправленное объяснение

До правки UI показывал structural_neighbors и betweenness только у coordinator. Поэтому по карточке распределителя с bw выше q99 или периферийного узла с достаточным числом соседей нельзя было объяснить, почему первое правило не выполнено. Теперь обе метрики видны всем ролям, рядом с формальным правилом видны точный порядок ролей и критерии coordinator/q99; экспорт также сохраняет числа и порядок. Формулы, назначения и CSV не изменялись.

Изменены только `aml/viewer_template.html` и `scripts/check_viewer.cjs`. Временный экран — `out-check/judge-probe/network.html`. Полный существующий DOM-check прошёл, включая два новых выбираемых по условиям контрпримера; отдельная проверка прошла все 11 сценариев. `out/`, документация и Git этим агентом не изменялись.

Ограничение: это проверка текста/данных и событий DOM. Читаемость длинной карточки, прокрутку и человеческую скорость ответа нужно подтвердить при живом просмотре.
