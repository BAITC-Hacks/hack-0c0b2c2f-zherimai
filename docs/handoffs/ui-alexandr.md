# Передача UI: Alexandr

Задача: проверить автономный экран и упростить объяснение приоритета в карточке.

- Ветка: `codex/ui-alexandr` от `680769e`.
- Изменённый модуль: `aml/viewer_template.html`.
- Человек (Alexandr) сообщил, что исходный экран `out/network.html` открылся и выводит данные. Это подтверждение относится к экрану до описанного ниже изменения.
- В обновлённой карточке показываются пять уже рассчитанных вкладов в `priority_score`, скидка для seed и базовая роль, если она отличается от итоговой. Баллы в UI не пересчитываются. В карточке и таблице приоритет отображается с четырьмя знаками вместо двух.
- После сообщения Alexandr, что блок «Состав приоритета» не виден, карточка первого кандидата стала открываться сразу при загрузке. Блок перенесён прямо под числовое объяснение, перед длинным списком остальных метрик. Режим обзора графа при загрузке сохраняется.

Проверки на Windows 10 x64, Python 3.14.7, Node 24.19.0:

- Последний `python -B run.py --data data --out out-check/ui`: PASS, 2,3526 с по отчёту, 2 248 узлов, 49 групп, топ-50. Все пять CSV побайтно совпали с `out/`; изменился только HTML.
- JavaScript нового HTML проходит синтаксическую проверку и проверку взаимодействий с DOM-заглушкой: поиск coordinator, изолированного seed и узла depth=4, состав приоритета, базовая роль, множитель seed, два шага, фильтр transit и неизвестный gid.
- Повторная проверка DOM-заглушкой подтвердила, что карточка топ-1 открыта без поиска, заголовок «Состав приоритета» расположен сразу после evidence и до прочих метрик, а общий обзор сети остался активным.
- Для топ-20 проверены выполнение минимального правила роли и сумма пяти сохранённых вкладов приоритета; все 20 совпали.
- Browser Use отклонил открытие нового локального HTML по политике `file://`. Визуальный просмотр именно обновлённой карточки в настоящем браузере не подтверждён.

Независимая проверка воспроизводимости из чистого архива коммита `680769e`: два полных прогона с установленными системными пакетами Python 3.14.7 прошли за 2,3125 и 2,5249 с; все пять CSV и HTML совпали побайтно, валидатор и 10 тестов прошли. Строгая офлайн-установка по README на этой Windows-машине не прошла: Python 3.13 отсутствует, локальных wheels закреплённых пакетов нет. Это результат проверки, который нужно учесть интегратору при обновлении общей документации; зависимости не менялись.

После интеграции капитану нужно пересоздать отслеживаемый `out/network.html` и обновить общий статус проверок. Финальный push в `main` и отдельная подача кейса остаются у капитана.


## Дополнение 23.09.2026, 16:32 Астана — совместимость со свежим main

Задача / участник: оставшиеся улучшения карточки и навигации, Alexandr с помощью Codex.
Ветка: `codex/ui-alexandr`. Перед доработкой выполнен fast-forward на `c31e093` из официального `main`; прежние UI-коммиты `d553d83` и `3ff778d` уже интегрированы капитаном.

Изменённые файлы этой передачи: `aml/viewer_template.html`, новый `tests/test_viewer.py`, эта запись. `aml/viewer.py`, расчётные модули, интерфейс `build_viewer`, зависимости, общие документы и отслеживаемые `out/` не изменены относительно `c31e093`.

Что изменилось для аналитика:

- Карточка и TXT показывают рассчитанное участие в направленном цикле; отсутствие значения обозначено «н/д».
- TXT сохраняет базовую роль, временное сопоставление, максимум плательщиков за день и индивидуальные ограничения. Пояснения и ограничения формируются общими функциями для экрана и TXT.
- Сохранены точный `next_request`, все сработавшие роли, диагностика временной достижимости и устойчивости из свежего main. Отдельная копия расчёта запросов данных не добавлялась.
- Исключение выбранного клиента фильтром роли/кластера теперь явно отмечается над карточкой; при возврате к связям или снятии фильтра предупреждение исчезает.
- При явном выборе клиента в узком окне (до 850 px) фокус и прокрутка переходят к заголовку карточки. Первоначальная загрузка не прокручивает страницу.
- Ссылки CSV доступны и при узком окне; заголовок переносится по ширине. Устаревшая подсказка графа скрывается при выборе другого клиента.
- Пояснение о снижении силы consolidator на четвёртом колене выводится только для фактической границы обхода.

Проверки на Windows 10 x64 / Python 3.14.7 / Node 24.19.0, с уже установленными пакетами:

- `python -B run.py --data data --out out-check/ui`: PASS, **2,9089 с**, 2 248 узлов, 49 групп, топ-50. Встроенный валидатор прошёл.
- Все **семь CSV** (`nodes_roles`, `clusters`, `top_nodes`, `data_requests`, `resilience`, `node_stability`, `sensitivity`) побайтно совпали с отслеживаемым `out/` коммита `c31e093`.
- `python -B -m unittest discover -s tests -v`: **93/93 PASS**. Три новые проверки: точность длинных gid; дословная передача `next_request`; безопасное встраивание недоверенных строк. Для Windows Temp тесты выполнены вне sandbox; предупреждения существующих тестов не приводили к ошибкам.
- `node scripts/check_viewer.cjs out-check/ui/network.html`: PASS — существующая регрессия капитана, включая диагностические карточки, TXT и строгие команды помощника. Сам скрипт не менялся.
- Дополнительная программная проверка `node out-check/ui-dom-audit.cjs out-check/ui/network.html`: **16/16 PASS** — входящие/исходящие соседи, два шага, неизвестный gid, изолят/depth=4, оба фильтра, цикл, TXT, недоверенные строки и вызовы фокуса/прокрутки при 700 px. Вспомогательный harness локальный, не входит в Git.
- Функции встроенного помощника и диагностического отображения побайтно сохранены из `c31e093`. `git diff --check` прошёл.

Ограничения: это программная проверка с DOM-заглушкой, не визуальный просмотр. Alexandr подтвердил предыдущую проверку UI `3ff778d`; новая компоновка в узком окне человеком ещё не подтверждена. Явный запрет Browser Use на `file://` не обходился. Чистая установка по обновлённому README этим прогоном не проверялась.

Следующий шаг капитану: принять эту UI-ветку поверх `c31e093` или более свежего main, пересоздать финальный `out/` и повторить `node scripts/check_viewer.cjs out/network.html`. Проверить новую карточку и скачивание TXT визуально; обновление общего TASKS и финальная подача остаются у интегратора.


## Уточнение 23.09.2026 — запуск без автоматически выбранного клиента

По прямому запросу Alexandr отменён первоначальный выбор первого клиента. Эта настройка заменяет описанный выше автоматический показ top-1. Перед правкой UI-ветка обновлена fast-forward до `d28e885`; изменения капитана сохранены.

- В `aml/viewer_template.html` удалена только строка выбора/открытия top-1 из `setup()`. При обычном открытии — вся сеть, без выделения и с приглашением выбрать клиента.
- Поиск, выбор на графе/в таблице, кнопка «Первый в очереди» и явная ссылка `#gid=...` сохранены.
- Существующая UI-регрессия `scripts/check_viewer.cjs` адаптирована к новой настройке: проверяет отсутствие начального выбора и выделенной строки, затем ручное открытие первого кандидата. Все последующие проверки сохранены.
- `python -B run.py --data data --out out-check/ui`: PASS, 2,9706 с; 2 248 узлов, 49 групп, топ-50. Семь CSV побайтно совпали с `out/` актуального main.
- `node scripts/check_viewer.cjs out-check/ui/network.html`: PASS, включая девять карточек всех шести ролей, TXT, фильтры, узкий экран и строгие команды помощника. Это программная проверка, не визуальный просмотр.

Капитану: принять коммит из `codex/ui-alexandr`, пересоздать финальный `out/` и запускать обновлённый скрипт UI-проверки. Отслеживаемые `out/`, расчётные модули и общие документы в этой передаче не менялись.


## Уточнение 23.09.2026 — короткий README и запуск на Windows

По явному запросу Alexandr отредактирована документация запуска и подготовлен push. Перед правкой выполнен fast-forward до `5926dbd`; сохранены новое название DalaAI и актуальные материалы капитана.

- В новом README Windows-путь через uv уже существовал. Добавлен отдельный простой путь для установленного Python 3.13 и одна команда повторного расчёта. Пути PowerShell отделены от macOS/Linux.
- Наверх вынесены просмотр готового `out/network.html`, пересчёт и основные результаты; дополнительные материалы размещены в 11 закрытых `<details>`. Все 18 содержательных разделов прежней версии, кроме переработанного быстрого запуска, сохранены полностью (таблица результатов перенесена в подробности).
- В Windows-блоках проверяется код выхода до `Start-Process`, чтобы ошибка расчёта не приводила к открытию старого HTML. Подробный `docs/install.md` согласован с README.
- Проверки: сохранность технических разделов; существование локальных ссылок и якорей; парность блоков кода/details; разбор трёх PowerShell-блоков штатным парсером; `git diff --check` — PASS. Независимое редакционное ревью — PASS. Новая установка, расчёт и визуальный рендер документа не выполнялись; приведённые 4,79 с / 98 тестов относятся к сохранённому протоколу капитана.

Изменены только `README.md`, `docs/install.md` и эта запись. Расчёт, интерфейс, зависимости и итоговые `out/` не менялись. Капитану достаточно принять документационный коммит из `codex/ui-alexandr`; пересчёт из-за него не нужен.
