# Независимая Windows QA: main и UI Александра

Дата: 23.09.2026. Участник C — Erzhan; проверки выполнены с Codex. Эта запись передаёт результаты интегратору; production-код не изменён.

## Проверенные версии и итог

- **Main:** `cc2c0b0589a3f818c0ad37d548de219ddfb62da9` (`cc2c0b0`). Полный расчёт, отдельный валидатор, независимый аудит и 49 тестов прошли. Семь CSV и HTML воспроизводимы побайтно.
- **UI Александра:** `d553d83fb1d15b9d6db3517d4dc3cb5102d02139` (`d553d83`), ветка `codex/ui-alexandr`, база `680769e`. Полный расчёт и восемь проверок с DOM-заглушкой прошли; пять CSV не изменились. Этот commit не входит в проверенный main.
- **Открытый дефект валидатора:** на `cc2c0b0` шесть ранее описанных подмен CSV всё ещё ошибочно получают PASS. Исправление в полученных версиях отсутствует; со слов Ержана, команда уже работает над ним.
- **Новая задача UI:** карточка `d553d83` пока не показывает диагностические поля, добавленные расширенным main. Визуальный просмотр обновлённой карточки не подтверждён.

Ветка передачи этого отчёта: `codex/qa-erzhan`, от `cc2c0b0`. Изменены только этот Markdown и соседний JSON с краткими протоколами. Основной checkout, staged `explore_edges.py`, production-код и его отслеживаемые результаты этим QA-пакетом не изменялись. Сообщения в чаты команды не отправлялись.

## Среда и границы проверки

Windows 11, сборка 26200, AMD64; CPython **3.13.15**. Окружение `out-check/qa-env` установлено Ержаном ранее: pandas 3.0.6, PyArrow 25.0.1, NetworkX 3.7, NumPy 2.5.3, SciPy 1.18.1, python-dateutil 2.9.0.post0, six 1.17.0; транзитивный tzdata 2026.4. Синтаксис/DOM UI проверены Node **24.19.0**.

Оба commit экспортированы через `git archive` в отдельные папки, **без извлечения `out/`**. Результаты созданы заново из исходных parquet. Использовано ранее установленное окружение Python; новой установки зависимостей и проверки Windows wheelhouse без сети в этом ходе не было. Расчёты и тесты не вызывали реальные внешние AI API; AI-тесты используют подмену HTTP. Проверка не заявляется нагрузочным benchmark: отдельные независимые проверки выполнялись параллельно.

## Main cc2c0b0

| Проверка | Фактический результат |
|---|---|
| Первый расчёт из чистой копии без out | PASS, 3,2116 с по пайплайну; **3,4308 с** по внешнему таймеру процесса |
| Повтор в другую папку | PASS, 3,3704 с; **3,6406 с** по внешнему таймеру |
| Отдельный validate.py | PASS |
| scripts/audit_top.py | Exit 0, `mismatches={}` |
| unittest с ResourceWarning как ошибкой | **49/49 OK**, 1,614 с внутри unittest |
| 7 CSV между двумя прогонами | Все совпали побайтно |
| 7 CSV против сохранённого out из архива cc2c0b0 | Все совпали побайтно |
| HTML между прогонами и с архивом | Побайтно совпал |
| gid/role/base_role/role_score/priority_score/cluster_id против предыдущего Windows-прогона 1c3710e | Все значения сохранены |

Независимый аудит подтвердил 19 сохранённых изолятов, отсутствие terminal на depth=4, максимальную длину evidence 124 символа, отсутствие обрезанных на 200 символах объяснений. Максимальное расхождение временной доли с независимым расчётом: 4,972966483052232e-13.

Команды из экспортированной папки, с указанием установленного интерпретатора в `$qaPython`:

```powershell
& $qaPython run.py --data data --out out
& $qaPython validate.py --data data --out out
& $qaPython scripts/audit_top.py
& $qaPython run.py --data data --out out-repeat
& $qaPython -W error::ResourceWarning -m unittest discover -s tests -v
```

В этой сессии `$qaPython` соответствует `out-check/qa-env/Scripts/python.exe` относительно исходного checkout. Штатные тесты выполнялись вне песочницы инструмента из-за ранее подтверждённого ограничения прав на TemporaryDirectory внутри неё. В обычном терминале Ержана прежний набор тестов также проходил.

Локальная диагностика сохранена в `out-check/qa-upstream/main-review/`: stdout/stderr шагов, summary.json, SHA-256 семи CSV. Эта папка игнорируется Git; переносимые итоговые факты продублированы в JSON рядом с данным отчётом.

## UI d553d83

Прочитан `docs/handoffs/ui-alexandr.md` из ветки Александра. Изменение показывает пять уже рассчитанных вкладов приоритета, множитель seed, базовую роль при её отличии от итоговой; приоритет отображается с четырьмя знаками. Баллы интерфейс заново не классифицирует.

Наш полный прогон на Windows / Python 3.13.15: PASS, **3,3279 с** по пайплайну; 2 248 узлов, 49 групп, топ-50. Все пять CSV совпали побайтно с `out/*` из архива UI-коммита.

Дополнительно проверены точные строковые gid всех 2 248 узлов и концов 3 119 рёбер против parquet. `node --check` прошёл. Восемь DOM-сценариев проверяют: карточку coordinator с пятью сохранёнными вкладами и базовой ролью; seed-множитель и отсутствие связей у изолята; ограничение depth=4; окрестности одного и двух шагов по исходным рёбрам; фильтр transit; неизвестный gid; четыре знака приоритета в таблице.

**Это не визуальный просмотр браузера.** Переданный Ержаном скриншот и человеческая проверка, описанная Александром, относятся к старой карточке. Стрелки и вёрстка новой карточки глазами не проверены.

### Что ещё требуется по новому назначению B

В `d553d83` шаблон ещё не использует:

- `matched_roles`, `next_request`;
- `temporal_seed_reach_strict_days`, `temporal_seed_reach_upper`;
- `cluster_stability`, `cluster_stability_scope`;
- `priority_top20_frequency`, `priority_min_rank`, `priority_max_rank`.

Подпись `seed_reach` остаётся «Достижимость от seed», без уточнения «структурно, без дат». `viewer.py` идентичен проверенному main и передаёт все колонки, поэтому проблема находится в отображении карточки, а не в схеме сериализации. Изоляты со scope `isolate_not_assessed` нельзя показывать как доказанную устойчивость 100%.

После интеграции B требуется пересоздать HTML из расширенного результата и пройти визуальный сценарий на точном commit. UI другой участник в этом QA-пакете не переписывал.

## Приёмка исправления валидатора пока не пройдена

`aml/validate.py` и `aml/assistant.py` в `cc2c0b0` идентичны версиям `1c3710e` по Git blob. Новые тесты не вызывают `validate_outputs`: прохождение 49 тестов не закрывает описанные подмены.

Контрольные изменения выполнялись только в копиях свежего результата. Импорт проверяемого валидатора явно направлен в snapshot `cc2c0b0`.

| Сценарий | Ожидается | Фактически |
|---|---|---|
| Исходный результат | PASS | PASS |
| Подмена depth 4→3 | Отказ | **PASS** |
| Только terminal на depth=4 | Отказ | Отказ |
| depth 4→3 и роль terminal | Отказ | **PASS** |
| in_deg изолята 0→1 | Отказ | **PASS** |
| Только consolidator у изолята | Отказ | Отказ |
| in_deg изолята 0→1 и consolidator | Отказ | **PASS** |
| Обмен is_seed между узлами одного кластера с сохранением n_seed | Отказ | **PASS** |
| Только transit у seed | Отказ | Отказ |
| Обмен is_seed и transit у исходного seed | Отказ | **PASS** |

Глубина/seed должны сверяться с `nodes.parquet`, степени/изоляты — с `edges.parquet`; запреты и число seed в кластере должны опираться на источник. После исправления исходный результат обязан проходить, остальные девять сценариев — отклоняться с понятной ошибкой.

Это дефект независимости валидатора, **не установленная ошибка текущих рассчитанных ролей**. SHA-256 данных и выходов до/после диагностики совпали. Точные изменения с выбранными из данных gid находятся в соседнем JSON, чтобы интегратор мог воспроизвести их без чата и игнорируемых папок.

## Следующие действия и ограничения

1. Интегратору: учесть подтверждённый Windows-запуск расширенного main в общих статусах и README, сохраняя границы проверки установки.
2. Владельцу исправления валидатора: опубликовать commit и закрыть девять отрицательных сценариев. C повторяет приёмку на точной версии.
3. Alexandr: обновить карточку под поля расширенного main, указать commit и результат визуального просмотра.
4. После объединения: полный пересчёт, аудит, тесты, актуальный HTML и финальная проверка. Финальный push и отдельная подача платформе остаются у капитана.

Linux, чистая офлайн-установка Windows, визуальный просмотр новой карточки и итоговая подача этим отчётом не подтверждаются. Реальный API-набор капитана повторно не запускался.
