# Уточнения после внешнего ревью

23.09.2026, интегратор a4ns с Codex. Источник замечаний — переданный капитаном текст Claude и сообщение Александра; это не наши собственные измерения.

- **UI Александра `3ff778d`:** капитан передал подтверждение проверки. Коммит уже находится в main (merge `4f162b6`), повторно объединять его не нужно. Более новые диагностические поля и строгие команды помощника требуют проверки свежего экрана.
- **Push:** на момент сверки GitHub и локальный HEAD совпадали: `8b36d99`. Коммиты `d553d83` и `ac39195`, названные во внешнем отзыве неопубликованными, уже входят в origin/main.
- **Вклад Ержана:** в main есть его независимый Windows QA `108e2a4` и исправление локального помощника с 23 тестами `ca8b43d`. Замечание только о двух ранних коротких README-коммитах относится к старому состоянию. Авторство сохранено.
- **Chromium/Linux:** по сообщению капитана Claude открыл предыдущий HTML, проверил поиск, карточку, очередь, отсутствие ошибок и внешних запросов. Проверенный SHA установить не удалось. Это полезное внешнее свидетельство, но не подтверждение текущего HTML или Python-пайплайна на Linux.
- **Python:** локальные метаданные подтверждают Python>=3.12 для закреплённых networkx/numpy/scipy. Прямая команда README выбирает именно python3.13; путь uv проверяется отдельно. Python 3.12 не объявляется поддержанным без полного прогона. Автоскачивание Python относится к сетевой подготовке, а не автономному расчёту.
- **Кодировка:** run.py, aml/validate.py и скрипт разведки явно используют UTF-8. Результаты и методика не меняются.
- **Первый экран README:** добавлены результат по ролям, топ-50, структурный сценарий удаления узлов и точное описание роли LLM.
- **Подача:** текст и инструкция готовы в `docs/submission.md`; ориентир — до 17:30, официальный дедлайн — 18:00. Фактическая отправка формы не подтверждена.

Папка `_to_delete` находится вне официального репозитория и содержит пустой `index.lock.stale`. Текущая работа Git не заблокирована; ради этой проверки чужие файлы не удалялись.
