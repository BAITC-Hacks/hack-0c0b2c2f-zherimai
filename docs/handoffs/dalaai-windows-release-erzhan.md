# DalaAI: Windows-приёмка и обновлённый автономный комплект

Участник: **Erzhan / C**. Проверка выполнена в сессии C средствами Python/Node.js, 23.09.2026 после 17:39 по Астане. Это программная проверка, не ручной просмотр интерфейса.

**Проверен точный commit `5926dbd12dc233093eb9b9b726cf114692f8079c`**: имя DalaAI, сокращённый README и старт экрана с обзора без выбранного клиента. Архив этого commit извлечён в новую папку без `out/`, `.venv/` и `.git/`; результаты рассчитаны заново из parquet.

Результаты, команды, версии и хеши: [переносимый протокол JSON](dalaai-windows-release-erzhan.json).

## Проверки

Среда: Windows 11 AMD64, CPython 3.13.15; закреплённые зависимости, Node.js 24.19.0 для отдельной DOM-регрессии. Использовано ранее проверенное окружение и прежние восемь wheels: `requirements.txt` не менялся. Новая установка зависимостей в этом прогоне не выполнялась.

| Проверка | Результат |
|---|---|
| Полный расчёт с встроенной валидацией | PASS; 3,9067 с по внешнему таймеру, 3,6560 с внутри приложения |
| Отдельный `validate.py` | PASS; 1,1520 с |
| Независимый `scripts/audit_top.py` | PASS; 2,3852 с; расхождений нет |
| `unittest discover -s tests -v` | 98/98 PASS; 4,967 с по unittest, 5,8464 с по внешнему таймеру |
| Предупреждения об утечках ресурсов | `ResourceWarning` обрабатывался как ошибка |
| `node scripts/check_viewer.cjs` | PASS; 0,5722 с; новая начальная пустая карточка и ручной выбор кандидата |
| Результаты относительно архивного `out/` | Все 7 CSV и HTML совпали побайтно, без необходимости нормализовать переносы строк |
| Изоляты / граница наблюдения | 19 изолятов сохранены; terminal на depth=4 отсутствуют |

Python-команды выполнялись через `tests/run_without_network.py`. Это перехват определённых socket-событий внутри процесса Python, а не системный firewall. Физическое отключение сети и сетевые обращения вне hooks этим способом не доказываются. API-запросы не выполнялись.

## Автономный Windows-комплект

Файл: **`dalaai-windows-py313-5926dbd.zip`**, размер **89 780 228 байт**.

```text
SHA-256: 4db2ee56422c5426d6ad19c0b3f32e25c1225508390268a1563eab10cda9ee30
```

Внутри — 88 исходных файлов проверенной версии, восемь совместимых wheels, инструкция `OFFLINE-WINDOWS.md` и манифесты. Всего 107 записей без дубликатов. Проверены SHA-256 исходников и wheels, CRC исходного архива, каждого wheel и итогового ZIP. Сам ZIP повторно проверен при подготовке этой передачи.

Отслеживаемые в Git готовые результаты не включены в комплект: расчёт создаёт их заново. Python, его установщик, Node.js, браузер и виртуальное окружение также не включены. Нужен **заранее установленный CPython 3.13 для Windows AMD64**. Для другой платформы требуется другой набор wheels.

Локальный комплект находится у C в игнорируемом каталоге `out-check/dalaai-release-5926dbd/`. Бинарный ZIP не отправлен в Git. Этот комплект заменяет предыдущий локальный ZIP `193feab` для демонстрации нового DalaAI; исторические архивы и отчёты сохраняются. Новые изменения после `5926dbd`, включая отдельную QA-проверку всех карточек, в него не входят.

## Передача и следующий шаг

- **Ветка:** `codex/qa-erzhan`; commit — коммит, добавляющий этот протокол и соседний JSON.
- **Изменённые файлы:** только два `dalaai-windows-release-erzhan.*` в `docs/handoffs/`; рабочий код, README и итоговый `out/` этим пакетом не меняются.
- **Границы:** 98 тестов относятся именно к `5926dbd`; DOM-заглушка не подтверждает визуальную компоновку, фактическое сохранение файла браузером или минутную репетицию. Это новая проверка исходной папки, но не новая установка окружения.
- **Капитану:** принять протокол в main и при необходимости взять локальный ZIP для передачи экспертам. При изменении исходников повторить проверку соответствующей версии. Ручной просмотр, человеческая репетиция и отдельное подтверждение подачи остаются самостоятельными действиями; данный отчёт не объявляет их выполненными.
